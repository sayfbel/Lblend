# Dependencies
node_modules/
dist/
build/
.DS_Store

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDEs
.vscode/
.idea/

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Database
server/database.sql
# (Exclude database.sql if you want to keep it out of git, but since the user has it in README, 
# maybe they want it. However, usually it's better to provide a schema file and keep actual data out.)
# I will keep database.sql for now unless it's huge, but I'll ignore general sql dumps.
*.sql

# Build outputs
client/dist/
server/dist/
